import { IComponent } from "../../types";
import "./user-details.css";

export class UserDetailsComponent implements IComponent {
  render(selector: string = "#user-details-container") {}
  addEvents(): void {}
}
